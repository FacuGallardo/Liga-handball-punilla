services:
  - type: web
    name: liga-handball-frontend
    env: node
    region: ohio
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run preview
    envVars:
      - key: NODE_ENV
        value: production
      - key: VITE_API_URL
        value: https://liga-handball-punilla-backend.onrender.com
    routes:
      - path: /
        destination: /index.html
